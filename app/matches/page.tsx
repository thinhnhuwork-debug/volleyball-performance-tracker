import Link from "next/link";import {AppShell} from "../../components/app-shell";import {PageHeader} from "../../components/ui";import {matches} from "../../lib/demo-data";
export default function MatchesPage(){return <AppShell><div className="page"><PageHeader eyebrow="MATCH CENTER" title="Trận đấu" description="Quản lý lịch thi đấu, kết quả và dữ liệu sau trận." action={<button className="primary-btn">＋ Tạo trận đấu</button>}/><div className="tabs"><button className="active">Tất cả</button><button>Đã kết thúc</button><button>Sắp tới</button></div><section className="panel">{matches.map(m=><Link href={`/matches/${m.id}`} className="match-row" key={m.id}><span className={`result ${m.result.toLowerCase()}`}>{m.result}</span><div><strong>HVC Volleyball vs {m.opponent}</strong><small>{m.date} · {m.competition} · {m.venue}</small></div><div style={{textAlign:"right"}}><b>{m.score}</b><small>{m.sets.join(" · ")}</small></div></Link>)}</section></div></AppShell>}
